import random


salary = int(input("Enter your current salary: "))
bonus = bool(random.randint(0, 1))

if bonus:
    total_salary = int(random.random() * salary) + salary
    print(f"{salary}, {bonus} - '{total_salary}'")
else:
    print(f"{salary}, {bonus} - '{salary}'")
